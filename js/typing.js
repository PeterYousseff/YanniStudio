const typedElement = document.querySelector('.typed');
const typed = new Typed (typedElement,{
    strings:
    [
        'Make Your Brand'
    ],
    typeSpeed:100,
    backSpeed:100,
    loop: false,
  loopCount: Infinity,
})